<style>
.flex-divs{
   display: flex;
   align-items: center;
   gap: 10px;
   justify-content: center;
   flex-wrap: wrap;
}

</style>

{{> header}}
{{> navbar }}
{{> form }}
<div class="snowdiv">
   <div class="flex-divs">
      {{selectBox id="pages" options='nd:Pages'}}
      {{createInput type="text" id="pagename" placeholder="Page Name"}}
      {{createInputTa type="text" id="description" placeholder="Description"}}
      {{checkBox id="statusKey" className="check" placeholder="Page Status" style=""}}
   </div>
   <input type="hidden" id="uniquekey" name="uniquekey">
   <input type="hidden" id="status" name="status" value="0">
<br>
<div class="flex-divs">
   <button id="addBtns" type="button">Add</button>
   <button id="pdfBtns" type="button">Print</button>
</div>

</div>

{{> formend }}
{{> view }}
<div id="lazy-contents">
</div>
<div id="loading">
</div>
{{> viewend }}
{{> dtb }}

<script nonce="{{nonce}}">
   const thead = `<th>S.no</th><th>Route</th><th>Pagename</th><th>Status</th><th>Action</th>`;
   const tfoot = ``;
document.addEventListener("DOMContentLoaded", () => {
   selectLoad('pages', '/page-master/loaduser', content = 'application/json', undefined);
   
   fetchTableData('contentsOfTable', '1000', '1', 'contentsOfTable', '', '/page-master/select', '50vh', 'lazy-contents', undefined, thead, tfoot);
});

document.getElementById("pdfBtns").addEventListener("click", async () => {
 await openPDF("/page-master/pdf");
});

document.getElementById("addBtns").addEventListener("click", () => {
    const formData=getFormData();
    console.log(formData);
    backEndCall(undefined, "/page-master/insert", "POST", undefined, formData);
});
document.addEventListener("click", (event) => {
   debugger
   const clickedId = event.target.id;
   if(event.target.classList=="edit-icon")
   {
      let userDatas = JSON.parse(document.getElementById(clickedId).getAttribute('datas'));
      Sv("pagename",userDatas.pagename);
      Ss("pages",userDatas.path);
      Sv("description",userDatas.description);
      if(userDatas.status==1)
         document.getElementById("statusKey").click();
      Si("addBtns", "Update");
      document.dispatchEvent(edit_event);
   }
   if(event.target.classList=="delete-icon")
   {
      const deleteId = document.getElementById(clickedId).getAttribute('uniquekey');
      deleteKey = JSON.stringify({uniquekey: deleteId});
      const userConfirmed = window.confirm("Are you sure you want to delete this item?");
      if(userConfirmed)
      backEndCall(undefined, "/page-master/delete", "POST", undefined, deleteKey);
   }
   if(clickedId=="statusKey")
   {
      if(Id("statusKey").checked)
         Sv("status","1");
      else
         Sv("status","0");
   }
   if(event.target.classList.contains("is-select"))
   {
      let pagesval = Val("pages");
      let datas=[pagesval];
      $("#contentsOfTable").empty();
      fetchTableData('contentsOfTable', '1000', '1', 'loading', 'Loading...', '/page-master/select', '50vh', 'lazy-contents', datas, thead, tfoot);
   }
   if(event.target.classList=="just-check")
   {
      const updateSts = JSON.stringify({clickedId:clickedId});
      backEndCall(undefined, "/page-master/update", "POST", undefined, updateSts);
   }
});
</script>
{{> footer }}
